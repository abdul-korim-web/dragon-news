import React from 'react';
export const metadata = {
  title: "Login | Dragon News",
  description: "This page is a dragon news login page",
};
const Layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
}

export default Layout;
