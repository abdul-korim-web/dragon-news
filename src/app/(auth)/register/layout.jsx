import React from 'react';
export const metadata = {
  title: "Register | Dragon News",
  description: "This page is a dragon news register page",
};
const Layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
}

export default Layout;
